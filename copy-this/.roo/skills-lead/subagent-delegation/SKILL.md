# Subagent Delegation

Use when creating subtasks.

Choose mode:
- Spec Pro for requirements, docs, architecture, UX, API/data planning.
- Implement Pro for scoped implementation and tests.
- Debug Pro for failing tests, runtime errors, and unknown breakages.

Ask each subtask to return a concise summary: done, changed files, verification, failures/skips, risks, next step.

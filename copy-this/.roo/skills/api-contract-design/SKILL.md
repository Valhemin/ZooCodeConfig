TRIGGER: adding or changing APIs — designing endpoints, functions, or events with a cross-service or public contract
OUTPUT: compact — endpoint/function/event name, schema, auth, error codes, idempotency, pagination, backward compat, test examples
SKIP: internal implementation details with no public-facing or cross-service contract

---

# API Contract Design

Use when adding/changing APIs.

Include:
- Endpoint/function/event name and purpose.
- Request/response schema.
- Validation and auth rules.
- Error codes/states.
- Idempotency and pagination where relevant.
- Backward compatibility/migration notes.
- Tests and contract examples.
